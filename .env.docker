# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║           TAILSCALE MCP AGENT - DOCKER CONFIGURATION                      ║
# ╠═══════════════════════════════════════════════════════════════════════════╣
# ║  1. Rename this file: mv .env.docker .env                                 ║
# ║  2. Fill in the 4 required values below                                   ║
# ║  3. Run: docker-compose up -d                                             ║
# ║  4. Auth Tailscale: docker-compose exec tailscale tailscale up            ║
# ║  5. Open: http://localhost:7860                                           ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# ┌───────────────────────────────────────────────────────────────────────────┐
# │ REQUIRED - Fill these in!                                                 │
# └───────────────────────────────────────────────────────────────────────────┘

# 1. Your Anthropic API key (https://console.anthropic.com/)
ANTHROPIC_API_KEY=sk-ant-xxxxx

# 2. Remote server Tailscale IP (run 'tailscale ip' on your server)
REMOTE_HOST=100.x.x.x

# 3. SSH username on the remote server
REMOTE_USER=your-username

# 4. Path to your SSH key (on your local machine)
SSH_KEY_PATH=~/.ssh
SSH_KEY_FILENAME=id_ed25519

# ┌───────────────────────────────────────────────────────────────────────────┐
# │ OPTIONAL - Only change if needed                                          │
# └───────────────────────────────────────────────────────────────────────────┘

# SSH key passphrase (leave empty if none)
REMOTE_SSH_KEY_PASSPHRASE=

# SSH port (default: 22)
# REMOTE_SSH_PORT=22

# Tailscale auth key (skip manual auth step)
# TS_AUTHKEY=tskey-auth-xxxxx